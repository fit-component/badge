import React from 'react'
import Badge from 'fit-badge'

export default class Demo extends React.Component {
    render() {
        return (
            <Badge count={5}>
                213
            </Badge>
        )
    }
}